import {SafeAreaView, StyleSheet, View, Text } from "react-native";
import React from 'react'
import { StyleContainer, InnerContainer, PageLogo, PageTitle } from "../components/styles";

export default function LoginScreen() {
    return(
       <StyleContainer>
        <InnerContainer>
            <PageTitle>
              VISION by Aabhas
            </PageTitle>
        </InnerContainer>
       </StyleContainer>
    );
}
